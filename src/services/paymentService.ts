// Production integration will be implemented in a later phase.
// Do not place secrets in frontend.
// paymentService stub — Phase 1 uses mock data only via useAppStore.
export const paymentServicePlaceholder = () => {
  throw new Error("paymentService not implemented in Phase 1 (mock mode).");
};
