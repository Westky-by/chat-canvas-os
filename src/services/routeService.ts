// Production integration will be implemented in a later phase.
// Do not place secrets in frontend.
// routeService stub — Phase 1 uses mock data only via useAppStore.
export const routeServicePlaceholder = () => {
  throw new Error("routeService not implemented in Phase 1 (mock mode).");
};
