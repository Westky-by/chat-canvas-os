// Production integration will be implemented in a later phase.
// Do not place secrets in frontend.
// metaService stub — Phase 1 uses mock data only via useAppStore.
export const metaServicePlaceholder = () => {
  throw new Error("metaService not implemented in Phase 1 (mock mode).");
};
