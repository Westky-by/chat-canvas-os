// Production integration will be implemented in a later phase.
// Do not place secrets in frontend.
// lineService stub — Phase 1 uses mock data only via useAppStore.
export const lineServicePlaceholder = () => {
  throw new Error("lineService not implemented in Phase 1 (mock mode).");
};
