// Production integration will be implemented in a later phase.
// Do not place secrets in frontend.
// auditService stub — Phase 1 uses mock data only via useAppStore.
export const auditServicePlaceholder = () => {
  throw new Error("auditService not implemented in Phase 1 (mock mode).");
};
