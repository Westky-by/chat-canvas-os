// Production integration will be implemented in a later phase.
// Do not place secrets in frontend.
// telegramService stub — Phase 1 uses mock data only via useAppStore.
export const telegramServicePlaceholder = () => {
  throw new Error("telegramService not implemented in Phase 1 (mock mode).");
};
