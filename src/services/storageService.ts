// Production integration will be implemented in a later phase.
// Do not place secrets in frontend.
// storageService stub — Phase 1 uses mock data only via useAppStore.
export const storageServicePlaceholder = () => {
  throw new Error("storageService not implemented in Phase 1 (mock mode).");
};
