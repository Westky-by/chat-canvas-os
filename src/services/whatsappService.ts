// Production integration will be implemented in a later phase.
// Do not place secrets in frontend.
// whatsappService stub — Phase 1 uses mock data only via useAppStore.
export const whatsappServicePlaceholder = () => {
  throw new Error("whatsappService not implemented in Phase 1 (mock mode).");
};
